<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Maven coordinates -->
    <parent>
        <groupId>com.github.dragonnukkit</groupId>
        <artifactId>mineprotocol-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>
    <artifactId>mineprotocol-common</artifactId>

    <!-- Module information -->
    <name>MineProtocol Common</name>
    <description>The common MineProtocol module, it contains all the common code between different protocols.</description>

    <!-- Module dependencies -->
    <dependencies>
        <dependency>
            <groupId>com.github.dragonnukkit</groupId>
            <artifactId>mineprotocol-api</artifactId>
            <version>${project.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-buffer</artifactId>
            <version>4.1.24.Final</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>
</project>
